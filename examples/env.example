# Actual Budget connection
ACTUAL_SERVER_URL="https://actual.example.com"
ACTUAL_PASSWORD="replace-me"
ACTUAL_SYNC_ID="replace-me"

# Optional: only if your budget file is encrypted
ACTUAL_ENCRYPTION_PASSWORD="replace-me"

# Optional local cache/state paths
ACTUAL_DATA_DIR="$HOME/.cache/hermes/actual-budget"
ACTUAL_DEBT_PROFILE="$HOME/.hermes/state/actual-debt-profile.json"
ACTUAL_AI_REVIEW_PATH="$HOME/.hermes/state/actual-ai-review.json"

# Optional OpenAI-compatible LLM endpoint for actual-ai-style classification
ACTUAL_AI_BASE_URL="http://localhost:11434/v1"
ACTUAL_AI_API_KEY="replace-me"
ACTUAL_AI_MODEL="qwen3.5:9b"
